<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>Grocery Store Employee Management System</title>

    {{!--
    <link rel="stylesheet" href="/css/jass.css"> --}}
    <link rel="stylesheet" href="/css/style.css">
</head>

<body class="body">
    <nav class="navbar">
    <div class="dropdown" style="margin-left: 50px">
  <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
    User
  </button>
  <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
    <a class="dropdown-item-logout" href="/login">Logout</a>
    <a class="dropdown-item-profile" href="/profile">Profile</a>
  </div>
</div>
        <h1 class="navbar-brand fs-1 text-white">Logo Placeholder</h1>

    <button class="questions" style="margin-right: 50px" id="questions">Questions?</button>
</nav>
    <main class="container container-fluid mt-5">
        {{!-- <section class="d-flex justify-content-center">
            <div class="card border-dark mt-5 mb-3" style="width: 12rem;">
                <div class="card-body">
                    <h5 class="card-title text-center">Please log in to access employee information.</h5>
                </div>
            </div>
        </section>
        <div class="d-flex justify-content-center">
            <form style="width: 12rem;">
                <div class="form-group">
                    <label for="exampleUsername">Username</label>
                    <input type="username" class="form-control" id="exampleUsername" placeholder="Enter Username">
                </div>
                <div class="form-group">
                    <label for="exampleInputPassword1">Password</label>
                    <input type="password" class="form-control" id="exampleInputPassword1" placeholder="Enter Password">
                </div>
                <button type="submit" class="btn btn-primary mt-3 mb-5" href="/login">Submit</button>
            </form>
        </div> --}}
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a href="/profile">profile</a>
        <button class="no-button" id="logout">logout</button>
        {{else}}
        <a href="/login">login</a>
        {{/if}}
        <div>
            {{{ body }}}
        </div>
    </main>
    <footer>
        <h2 class="companyName text-center">Grady's Groceries</h2>
    </footer>
    </header>




    <!-- Render script for logged in users only -->
    {{!-- {{#if logged_in}} --}}
    {{!--
    <script src="/js/logout.js"></script> --}}
    {{!-- {{/if}} --}}
</body>

</html>